import pytest

def num_ways(num_dice, num_faces, wanted_sum):
    #get a list of the possible values of num dice and faces
    #itterated over that list to see if sums of those == wanted sum
    #save to another list
    #num ways = len of that list save of possible outcomes
    #can print the list as well to prove it works
    pass